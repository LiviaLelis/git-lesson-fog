{
  "name": "git-lesson",
  "scripts": {
    "build-html": "marp deck.md -o deck.html --theme rosepine.css --allow-local-files",
    "build-pdf": "marp deck.md -o deck.pdf --theme rosepine.css --allow-local-files",
    "dev": "marp -w deck.md --theme rosepine.css --allow-local-files",
    "start": "marp -w deck.md --theme rosepine.css --allow-local-files"
  },
  "packageManager": "pnpm@10.4.1",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}
